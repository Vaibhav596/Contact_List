<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/CSS/style.css">
    <title><%= title%></title>
</head>
<body>
    <h1>My Contact List</h1>
    <div class="container">
        <h1>Create Contact</h1>
        <form action="/create-contact" method="post">
            <input type="text" placeholder="Name" name="name"><br>
            <input type="text" placeholder = "Contact" name="contact"><br>
            <button type="submit" id="create">Submit</button>
        </form>
    </div>
    <div class="container2">
        <form action="/delete-contact" method="post">
            <button type="submit" id="delete">Delete</button>
            <ol>
                <% for(let contacts of contactList){%>
                    <input type="checkbox" name="<%= contacts._id%>" id="checkbox">
                    <li>
                        <p id="name"><%= contacts.name %></p>
                        <p id="contact"><%= contacts.contact %></p>
                    </li>
                <%}%>   
            </ol>
        </form>
    </div>
</body>
</html>